generator client {
  provider = "prisma-client-js"
}

model Opportunity {
  id          String   @id @default(cuid())
  title       String
  description String
  imageUrl    String
  location    String
  commitment  String
  organisation String
  createdAt   DateTime @default(now())
}